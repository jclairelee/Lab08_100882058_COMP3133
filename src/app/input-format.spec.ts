import { InputFormat } from './input-format.directive';

describe('InputFormat', () => {
  it('should create an instance', () => {
    const directive = new InputFormat();
    expect(directive).toBeTruthy();
  });
});
